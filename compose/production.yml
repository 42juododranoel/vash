version: '3.4'

services:
  django:
    environment:
      - GUNICORN_WORKERS_NUMBER
    volumes:
      - ${MEDIA_DIRECTORY}/:/resources/media/
    command: gunicorn vash.wsgi --reload -b :8000 --error-logfile - --access-logfile - -w ${GUNICORN_WORKERS_NUMBER}
